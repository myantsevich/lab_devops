[Unit]
Description=Simple Message Server
After=network.target

[Service]
Type=simple
EnvironmentFile=-{{ destin_file }}
ExecStart={{ msg_home }}bin/msg-server
ExecStop=/bin/kill -s QUIT $MAINPID
User={{ app_user }}
Group={{ app_group }}

[Install]
WantedBy=multi-user.target